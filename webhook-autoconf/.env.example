# Copie para ".env" (mesma pasta) e preencha com os dados reais.
# O ".env" nunca é enviado pro GitHub (está no .gitignore).

# Credenciais de integração API 2.x da LitoralCar, fornecidas pelo suporte
# deles (assistencia@litoralcar.com.br) em 11/09/2026, exclusivas pra este
# sistema (não reutilizar em outro lugar, conforme pedido deles).
LITORALCAR_USERNAME=
LITORALCAR_PASSWORD=
LITORALCAR_COD_LOJA=1989
